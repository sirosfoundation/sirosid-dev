# Generated PKI
fixtures/vc-pki/*.key
fixtures/vc-pki/*.crt
fixtures/vc-pki/*.pem

# Dependencies
node_modules/

# IDE
.idea/
.vscode/
!.vscode/mcp.json
*.swp

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Docker
.docker/
build-info.json

# Golden release
.env.golden
.golden-releases.yaml

# Android dev (generated by scripts/generate-assetlinks.sh)
.well-known/
fixtures/well-known/

# Android app package+key list (per-developer/per-checkout - see .android-apps.example)
.android-apps

# Android dev (generated by scripts/generate-android-config.py)
fixtures/vc-config-android.yaml
.env.android

# Cloudflare tunnels (generated by scripts/tunnel.sh)
.env.tunnel

# Tunnel-patched vc-config (generated by scripts/generate-tunnel-config.py)
fixtures/vc-config-tunnel.yaml

# Local-patched vc-config for plain `make up VC=yes` (no TUNNELS, no
# CONFORMANCE) - same generator, just pointed at the reachable in-network
# vc-apigw URL instead of a tunnel URL. See the Makefile's `up:` target.
fixtures/vc-config-local.yaml

# go-trust whitelist rendered from fixtures/go-trust-whitelist.txt with this
# machine's bridge-gateway vc-apigw URL added (Makefile `up:` target).
fixtures/go-trust-whitelist-local.json
.tunnel.pids
.tunnel-logs/
fixtures/.vc-config.tunnel.yaml
.env.android

# Conformance results (generated by run-android-usb-conformance.mjs)
conformance-results/
.env
conformance-runner/
fixtures/vc-config-android-usb.yaml

# Helm-rendered config + generated secrets (PDP=helm, scripts/render-helm-config.py)
fixtures/rendered/
fixtures/rendered-secrets/

# Python bytecode cache
__pycache__/
*.pyc
